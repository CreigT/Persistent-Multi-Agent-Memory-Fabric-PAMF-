# Locked sequence — five exist

1. PAMF → Postgres — CreigT/Persistent-Multi-Agent-Memory-Fabric-PAMF-
2. IdMA tokens — CreigT/IdMA
3. Event Trigger Agent — CreigT/AetherMesh-Control-Plane `src/event`
4. Governance + Warden freeze gate — same repo `src/governance` `src/warden`
5. One janitorial money loop — same repo `src/loop`

Caps: review $50 / freeze $250. Collect is a stub. No live Stripe.

The company *skeleton* exists. It is not a live business until collect hits a real rail and PAMF citations sit on every decision.
